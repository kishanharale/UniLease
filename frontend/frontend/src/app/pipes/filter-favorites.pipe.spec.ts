import { FilterFavoritesPipe } from './filter-favorites.pipe';

describe('FilterFavoritesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFavoritesPipe();
    expect(pipe).toBeTruthy();
  });
});
